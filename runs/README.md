# Runs

This directory is for generated self-play and version-battle outputs.

Expected generated subdirectories include:

- `runs/<run_id>/games/`
- `runs/<run_id>/memory_candidate/`
- `runs/<run_id>/dream_reports/`
- `runs/<run_id>/skill_proposals/`

Run outputs are ignored by Git. Keep only this README committed.

