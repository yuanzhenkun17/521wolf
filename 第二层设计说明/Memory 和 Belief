1. Memory 该怎么设计

Memory 主要存“已经发生过的事实”。

比如：

第 1 天，P3 发言说怀疑 P5
第 1 天，P6 投票给 P2
第 2 夜，P4 死亡
P7 曾经自称预言家
自己第 1 天投过 P5

Memory 不应该直接写：

P3 是狼

因为这是判断，不是事实。

所以 Memory 第一版可以设计成 事件列表：

class MemoryEvent:
    day: int
    phase: str
    event_type: str
    actor: str | None
    target: str | None
    content: str
    visibility: str

比如一条发言记忆：

{
  "day": 1,
  "phase": "day_discussion",
  "event_type": "speech",
  "actor": "P3",
  "target": null,
  "content": "P3 说 P5 发言很像狼",
  "visibility": "public"
}
2. Belief 该怎么设计

Belief 存“这个玩家自己的判断”。

比如：

我觉得 P3 有点像狼
我觉得 P6 可能是真预言家
我现在不太信 P8
我倾向站边 P6

Belief 可以围绕“每个玩家的身份概率/怀疑程度”来做。

第一版不需要太复杂，可以这样：

class PlayerBelief:
    player_id: str
    suspicion: float        # 狼人嫌疑，0~1
    trust: float            # 信任度，0~1
    possible_roles: dict    # 可能身份
    reasons: list[str]      # 判断理由

比如：

{
  "player_id": "P5",
  "suspicion": 0.75,
  "trust": 0.25,
  "possible_roles": {
    "werewolf": 0.7,
    "villager": 0.2,
    "seer": 0.1
  },
  "reasons": [
    "第 1 天发言前后矛盾",
    "投票跟随 P3，没有独立理由"
  ]
}
3. Memory 和 Belief 的关系

核心流程应该是：

Observation
    ↓
写入 Memory
    ↓
根据 Memory 更新 Belief
    ↓
根据 Belief 决策

举个例子：

观察到：P3 白天强烈攻击 P5

先进入 Memory：

第 1 天，P3 怀疑 P5

然后更新 Belief：

P3 和 P5 的阵营关系可能不一致
P3 对 P5 的攻击强度较高
P5 的狼面略微上升或下降，需要结合后续投票判断

注意：Memory 是原材料，Belief 是加工结果。

4. 每个玩家都要有自己的 Memory 和 Belief

这是非常重要的。

不是全局只有一个 Memory/Belief，而是：

P1 有 P1 的 Memory / Belief
P2 有 P2 的 Memory / Belief
P3 有 P3 的 Memory / Belief
...

因为每个玩家看到的信息不同、身份不同、目标也不同。

比如狼人 P3 的 Belief 里可以知道队友是谁；
村民 P4 的 Belief 里不能知道这个。